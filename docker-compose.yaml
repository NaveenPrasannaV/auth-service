version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: secureflow-postgres
    environment:
      POSTGRES_DB: secureflow
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"